import {Container} from '../assets/styles/styles';

export function Home() {

    return (
        <Container>
            <p>ELECTRON + REACT</p>
            <p>
                Now you can use the Electron framework to build desktop apps.
            </p>
        </Container>
    );
}